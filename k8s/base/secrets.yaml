apiVersion: v1
kind: Secret
metadata:
  name: printke-secrets
  namespace: printke
type: Opaque
stringData:
  # Database
  POSTGRES_PASSWORD: "printke123"
  DATABASE_URL: "postgresql://printke:printke123@postgres:5432/printke"

  # Redis
  REDIS_PASSWORD: "redis123"
  REDIS_URL: "redis://:redis123@redis:6379/0"

  # MinIO
  MINIO_ROOT_USER: "minioadmin"
  MINIO_ROOT_PASSWORD: "minioadmin123"
  S3_ACCESS_KEY: "minioadmin"
  S3_SECRET_KEY: "minioadmin123"

  # API Security
  SECRET_KEY: "k8s-secret-key-change-in-production-min-32-chars"
  JWT_SECRET_KEY: "k8s-jwt-secret-key-change-in-prod-min-32-chars"
